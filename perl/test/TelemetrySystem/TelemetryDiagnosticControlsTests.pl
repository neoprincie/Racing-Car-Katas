use strict;
use warnings;

use Test::More 0.96;

use lib 'lib/TelemetrySystem';

require TelemetryDiagnosticControls;

subtest 'checkTransmission should send a diagnostic message and receive a status message response' => sub {

};

done_testing();